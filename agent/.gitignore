# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/
env/
*.egg-info/
dist/
build/
node_modules/

# Test / coverage / linters
.coverage
htmlcov/
.pytest_cache/
.ruff_cache/
.hypothesis/
.tox/

# Runtime data (local research, session state, secrets)
data/
skills/active-skills.txt

# Runtime memory (created at startup, not tracked)
memory/conversations/
memory/dreamer/

# Dreamer state (runtime)
dreamer/

# Notifications (runtime) — anchored so it only ignores the runtime dir, not the `notifications` skill
/notifications/

# Logs
logs/

# Model files
*.bin
*.onnx
*.pt
*.gguf

# Databases
*.db
*.db-wal
*.db-shm
*.sqlite

# Media
*.mp3
*.mp4
*.wav
*.webm

# Archives
*.zip
*.tar.gz

# Build artifacts
skills/*/cli/build/
skills/dashboard/app/core

# Constitution: user-authored, bind-mounted read-only by vestad (per-agent, not
# workspace content). Anchored so it never lands in the workspace snapshot or git status.
/constitution.md
