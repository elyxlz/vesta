What's probably going on with them right now? Could you get started on any tasks, check in on something, or take care of anything quietly? Only reach out if something genuinely needs them — notifications handle the inbox and calendar, this is about being proactive with work and check-ins.

Always check their current location (`ha location`) — it gives useful context for everything else.

If they're not home, check the Blink alarm panels (`alarm_control_panel.blink_nuraghesopra`, `blink_nuraghesotto`, `blink_nuraghecantinetta`). If any are disarmed while they're away, warn them immediately.
