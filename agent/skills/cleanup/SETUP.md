# Cleanup Skill — Setup

No setup required.

The cleanup script uses only Python stdlib and pathlib. It works out of the box on any vesta instance:

```bash
python3 ~/vesta/skills/cleanup/scripts/cleanup.py
```
