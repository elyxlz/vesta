#!/bin/bash
# SMTP send. Runs in the persistent uv env at $EMAIL_CLIENT_DIR/runtime.
DIR="${EMAIL_CLIENT_DIR:-$HOME/.email-client}"
cd "$DIR/runtime" && exec uv run --quiet python3 "$DIR/smtp_send.py" "$@"
