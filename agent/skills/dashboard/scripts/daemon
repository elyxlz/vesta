#!/bin/sh
set -eu

# LEGACY(remove-when: the 2026-08-daemon-pidfile migration is fleet-applied): restart
# skills written before the daemon verb still launch the dashboard by this path, and the
# path outlives the sync that converts them, so it forwards rather than breaking.

exec "$HOME/agent/skills/dashboard/dashboard" daemon "$@"
