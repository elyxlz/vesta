# WhatsApp MCP Configuration

# Optional: WhatsApp Bridge API endpoint (defaults to http://localhost:8080/api)
# WHATSAPP_API_BASE_URL=http://localhost:8080/api

# Note: WhatsApp MCP requires a WhatsApp Bridge service running
# The bridge provides the connection to WhatsApp Web