import type { SheetChromeProps } from "@/components/sheet-chrome.types";

// iOS form sheets draw their chrome natively: the grabber and header title
// come from the route options and the close button from NativeSheetCloseButton.
export function SheetChrome(props: SheetChromeProps) {
  void props;
  return null;
}
