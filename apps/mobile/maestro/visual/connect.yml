appId: ${APP_ID}
name: Connection actions and link
tags:
  - visual
  - onboarding
---
- runFlow: prepare-page.yml
- setPermissions:
    permissions:
      camera: deny
- openLink:
    link: "vesta-dev://connect?visualPrivacy=unlocked"
- waitForAnimationToEnd:
    timeout: 3000
- tapOn:
    text: "^Open$"
    optional: true
    waitToSettleTimeoutMs: 1
- extendedWaitUntil:
    visible: ".*While [Uu]sing.*"
    timeout: 6000
    optional: true
- tapOn:
    text: ".*While [Uu]sing.*"
    optional: true
- extendedWaitUntil:
    visible: ".*[Oo]nly [Ww]hile [Uu]sing.*"
    timeout: 4000
    optional: true
- tapOn:
    text: "Keep Only While Using"
    optional: true
- runFlow:
    when:
      platform: Android
    commands:
      - extendedWaitUntil:
          visible: ".*Location access.*"
          timeout: 5000
          optional: true
      - runFlow:
          when:
            visible: ".*Location access.*"
          commands:
            - pressKey: Back
      - extendedWaitUntil:
          visible: ".*Turn on.*"
          timeout: 5000
          optional: true
      - tapOn:
          text: ".*Turn on.*"
          optional: true
- runFlow: wait-for-launch.yml
- takeScreenshot: connect-actions
- runScript:
    file: capture-screenshot.js
    env:
      SCREENSHOT: connect-actions.png

- tapOn:
    text: "Connect with Vesta Cloud"
    waitToSettleTimeoutMs: 1
- takeScreenshot: cloud-sign-in-error
- runScript:
    file: capture-screenshot.js
    env:
      SCREENSHOT: cloud-sign-in-error.png

- tapOn:
    text: "Connect to self-hosted Vesta"
    waitToSettleTimeoutMs: 1
- extendedWaitUntil:
    visible: "Connect your gateway"
    timeout: 10000
- waitForAnimationToEnd:
    timeout: 3000
- tapOn: "Connection link"
- waitForAnimationToEnd:
    timeout: 3000
- runScript:
    file: capture-screenshot.js
    env:
      ACTION: show-software-keyboard
- waitForAnimationToEnd:
    timeout: 3000
- takeScreenshot: connect-link
- runScript:
    file: capture-screenshot.js
    env:
      SCREENSHOT: connect-link.png

- setPermissions:
    permissions:
      camera: deny
- tapOn:
    text: "Scan connection QR code"
    waitToSettleTimeoutMs: 1
# iOS shows the denied recovery state directly; Android asks again through the
# system dialog, and only a second denial makes the denial permanent, so deny
# until the app's recovery state appears.
- runFlow:
    when:
      platform: iOS
    commands:
      - extendedWaitUntil:
          visible: ".*Open Settings"
          timeout: 10000
- runFlow:
    when:
      platform: Android
    commands:
      - tapOn:
          text: "Don’t allow"
          waitToSettleTimeoutMs: 1
      - tapOn:
          text: "Don’t allow"
          optional: true
          waitToSettleTimeoutMs: 1
      - extendedWaitUntil:
          visible: ".*Open Settings"
          timeout: 10000
      - waitForAnimationToEnd:
          timeout: 3000
- takeScreenshot: scan-permission
- runScript:
    file: capture-screenshot.js
    env:
      SCREENSHOT: scan-permission.png
# Invalid-link pixels must not depend on a prior scanner dismissing. The
# scanner round trip is covered separately by navigation/connect-scanner.yml.
- stopApp
- openLink:
    link: "vesta-dev://connect-link?visualPrivacy=unlocked"
- runFlow: wait-for-launch.yml
- tapOn: "Connection link"
- inputText: "not-a-link"
# Finish keyboard/layout work before triggering the short-lived error toast.
- runScript:
    file: capture-screenshot.js
    env:
      ACTION: show-software-keyboard
- waitForAnimationToEnd:
    timeout: 3000
- tapOn:
    text: "^Connect$"
    waitToSettleTimeoutMs: 1
- runScript:
    file: capture-screenshot.js
    env:
      SCREENSHOT: connect-link-invalid.png
- takeScreenshot: connect-link-invalid
